# Mobile responsiveness fixes

## Changes
- Constrain the shared app shell and mock-data page to the viewport with responsive side spacing and horizontal overflow protection.
- Rework the top navigation into a width-safe row, with a compact search field on phones and fixed-size icon controls.
- Let prompt presets and result actions wrap naturally on narrow screens.
- Keep generated tables inside their own horizontal swipe area without widening the page.

## Verification
- Check the preview at mobile and desktop widths for horizontal page scrolling, clipped controls, and table swipe behavior.
- Confirm the current build remains error-free.
